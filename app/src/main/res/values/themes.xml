<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light, chrome-less. 2016 YouTube had no dark mode, so neither do we. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Darker shade of the 2017 red app bar. MainActivity re-applies this
             at runtime, since dark mode can be toggled without recreating the
             activity and these static values would go stale. -->
        <item name="android:statusBarColor">#CC181E</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
