<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light, chrome-less. 2016 YouTube had no dark mode, so neither do we. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Darker shade of the red app bar, as the 2016-17 app had it. For
             the white web bar instead, use #F1F1F1 with
             windowLightStatusBar true. -->
        <item name="android:statusBarColor">#CC181E</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
